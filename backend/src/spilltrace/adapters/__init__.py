"""Adapters — every external boundary lives behind a Protocol declared in
``spilltrace.core.ports``.  Each port has a real implementation and a deterministic
implementation so the whole pipeline runs without any external provider (FR-020).
"""
