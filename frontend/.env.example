# Browser-visible configuration. These values are inlined into the client bundle,
# so they must NEVER contain a credential of any kind (CON-004).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=SPILLTRACE
